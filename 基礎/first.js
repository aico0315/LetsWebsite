'use strict';

window.alert('はじめてのJavaScript!'); //「window.」の部分は省略可能。 alert('はじめてのJavaScript!'); でも同じように動作する
                                     // ※ windowオブジェクトに限り省略が可能 その他history,location,document,consoleなどのオブジェクトは省略不可。